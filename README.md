# Gulp Svg Sprite Task
Creates svg symbol sprites (monocolor and multicolor)

## How to start
* `yarn` or `npm i` - install npm dependencies
* `gulp` - run in watch mode
* `gulp build` - run and stop
